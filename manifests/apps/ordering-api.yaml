apiVersion: apps/v1
kind: Deployment
metadata:
  name: ordering-api
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ordering-api
  template:
    metadata:
      labels:
        app: ordering-api
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: ordering-api
          image: git.local:3000/gitea/ordering-api:latest
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: ConnectionStrings__orderingdb
              value: Host=postgres;Database=orderingdb;Username=postgres;Password=postgres
            - name: ConnectionStrings__eventbus
              value: amqp://guest:guest@rabbitmq:5672
            - name: Identity__Url
              value: http://identity-api:8080
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "256Mi"
              cpu: "200m"
            requests:
              memory: "128Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: ordering-api
  namespace: default
spec:
  selector:
    app: ordering-api
  ports:
    - port: 8080
      targetPort: 8080
