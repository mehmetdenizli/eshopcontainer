apiVersion: apps/v1
kind: Deployment
metadata:
  name: basket-api
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basket-api
  template:
    metadata:
      labels:
        app: basket-api
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: basket-api
          image: git.local:3000/gitea/basket-api:latest
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
            - name: ConnectionStrings__redis
              value: redis:6379
            - name: ConnectionStrings__eventbus
              value: amqp://guest:guest@rabbitmq:5672
            - name: Identity__Url
              value: http://identity-api:8080
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "256Mi"
              cpu: "200m"
            requests:
              memory: "128Mi"
              cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: basket-api
  namespace: default
spec:
  selector:
    app: basket-api
  ports:
    - port: 8080
      targetPort: 8080
